# SP003 — 应用生命周期与重启压力

[English](README.md) | [简体中文](README.zh-CN.md)

SP003 是基准测试自有的 cFS 运行时压力应用。其配置针对 cFE Executive Services 执行受限 CPU 压力、EVS/syslog 突发、应用退出、重启/删除/重新加载请求及受控崩溃循环。

成功必须有请求的运行时效果对应的 cFS/ES 证据；ES 拒绝会记录为防御。尤其是调度器饥饿配置，只有 SCH 遥测显示调度影响时才会通过，忙循环本身不足以构成成功。该包包含组件、字典、安装器、验证器和卸载器。
